# Grafika 学习路线（小白从浅入深版）

这份文档面向**完全新手**：你不需要一上来就懂 OpenGL / MediaCodec。我的目标是让你用最少的前置知识，循序渐进地“看懂、跑通、能改、能迁移”Grafika 里的核心套路。

> 你现在仓库里已经有一些很好的资料：
>
> - `Grafika_学习指南.md`：更偏“代码结构导览”（适合先快速浏览一遍）
> - `FBO小白.md`：更偏“FBO + 录制”主题的手把手讲解（偏实战）
>
> 本文是把它们**串成一条学习路径**，并且每一步都回答两个问题：
> - **为什么要这么做？**
> - **下一步应该看哪里 / 改哪里？**

---

## 0. 你先要建立的“脑内地图”（5 分钟）

把 Grafika 先粗暴分成两条主线，你学起来就不会迷路：

- **渲染线（OpenGL/EGL）**：把“像素”画出来，并能画到屏幕/纹理/编码器
  - 关键词：`EGLDisplay / EGLContext / EGLSurface`、着色器、纹理、FBO
- **编码线（MediaCodec/MediaMuxer）**：把“像素帧”压缩成 H.264，再封装成 mp4
  - 关键词：`MediaCodec`、`Surface` 输入、`MediaMuxer`、时间戳（presentation time）

**为什么先分主线？**  
因为很多人学不下去的原因是：把“GL 怎么画”与“视频怎么编码”混成一团看。先拆开，你每次只解决一个问题。

**下一步**：先跑起来，验证环境没问题。

---

## 1. 第一关：先跑起来（不理解也能先跑）✅

### 你要做什么

- 用 Android Studio 打开工程
- 连接真机（更推荐真机，模拟器的 MediaCodec/GL 表现可能不一致）
- 直接运行 App
- 在列表里点几个 Activity 看效果

### 为什么这么做

Grafika 是“实验场”，很多内容是为了验证系统行为。**先看到效果**，再回头看代码，你会一直知道“这段代码在干嘛”。

### 推荐你先点的 3 个 Activity（从最轻到最硬）

- `GlesInfoActivity`：看看设备支持的 GLES 版本/扩展（先建立现实感）
- `TextureViewGLActivity`：最简单的“在 TextureView 上画 OpenGL”
- `RecordFBOActivity`：Grafika 的“王牌关卡”，同时预览 + 录制

**下一步**：开始走渲染线：先搞明白 EGL 在 Android 到底做了什么。

---

## 2. 第二关：先学会“把 GL 画到屏幕上”（渲染线入门）🎨

### 你要读的文件（按顺序）

1) `app/src/main/java/com/android/grafika/gles/EglCore.java`  
2) `app/src/main/java/com/android/grafika/gles/EglSurfaceBase.java`  
3) `app/src/main/java/com/android/grafika/gles/WindowSurface.java`  
4) `app/src/main/java/com/android/grafika/gles/GlUtil.java`

### 你要搞懂的 4 句话

- **EGLContext**：GL 的“工作间”（你的 gl* 调用在哪个上下文里执行）
- **EGLSurface**：GL 的“画布”（画到屏幕 Surface，或离屏 pbuffer）
- **makeCurrent**：把“工作间 + 画布”绑定到当前线程（绑定后才能安全地 glDraw）
- **swapBuffers**：把这帧“提交出去”（屏幕显示/编码器接收的关键动作）

### 为什么要先学这些

新手最容易踩的坑：**“我明明调用了 glDraw，为什么没东西？”**  
答案常常是：你没 `makeCurrent()`，或在错误线程/错误 surface 上画。

### 一个最小练习（强烈推荐）

在 `TextureViewGLActivity` 里找到绘制循环，尝试做一个“肉眼可见的改动”：

- 把背景色 `glClearColor(r,g,b,a)` 改掉
- 或者让某个参数随时间变化（比如颜色随帧数变化）

**为什么要做练习？**  
因为你一旦能“改一行→看到变化”，你就从“看天书”进入“能控制系统”的阶段了。

**下一步**：你已经能画到屏幕了，现在学会“把画面当成纹理再画一次”（纹理/全屏四边形）。

---

## 3. 第三关：理解“纹理是什么？外部纹理又是什么？”🧩

### 你要读的文件

- `app/src/main/java/com/android/grafika/gles/Texture2dProgram.java`
- `app/src/main/java/com/android/grafika/gles/FullFrameRect.java`
- `app/src/main/java/com/android/grafika/gles/Drawable2d.java`

### 你要建立的直觉

- **纹理**就是 GPU 上的一张图（可以来自图片、来自摄像头、来自你离屏渲染的结果）
- `FullFrameRect` 做的事很简单：**用一个“铺满屏幕的矩形”，把纹理贴上去**

### 为什么这一步很关键

FBO 的核心套路就是：**先把场景渲染到纹理**，然后把这张纹理：

- 贴到屏幕给用户看（预览）
- 再贴到编码器的 `Surface`（录制）

如果你不理解“纹理→再画一次”，后面 FBO 会完全看不懂。

**下一步**：进入编码线：先理解 MediaCodec 的最小闭环。

---

## 4. 第四关：先把“视频编码最小闭环”跑通（编码线入门）🎞️

### 你要读的文件（按顺序）

- `app/src/main/java/com/android/grafika/VideoEncoderCore.java`
- `app/src/main/java/com/android/grafika/TextureMovieEncoder.java`
- `app/src/main/java/com/android/grafika/TextureMovieEncoder2.java`

### 你要搞懂的最小闭环

- `MediaCodec` 配置为 `COLOR_FormatSurface` 后，会给你一个**输入 Surface**
- 你只要能把画面画到这个 Surface 上，并 `swapBuffers()` 提交帧
- 编码器就会在后台把帧压缩成 H.264
- `MediaMuxer` 再把 H.264 包进 mp4

### 为什么 Grafika 要把编码放到线程里

编码/写文件是“慢操作”。如果你把它放 UI 线程，会卡顿、掉帧、ANR。  
Grafika 的实现里非常强调：**渲染线程做渲染，编码线程做 drain/mux**。

**下一步**：两条线合流：开始读 `RecordFBOActivity`，把“预览 + 录制”一次性串起来。

---

## 5. 第五关（主线关卡）：RecordFBOActivity —— 一次绘制，两处输出 🏆

### 你要读的文件

- `app/src/main/java/com/android/grafika/RecordFBOActivity.java`

### 你要抓住的“主线流程”

你只需要盯住这条流程（先别被细节吓到）：

1) **准备 EGL**：创建显示用的 `WindowSurface`
2) **准备 FBO**：创建离屏纹理 + framebuffer（你已经在注释里看到详细解释了）
3) **每帧绘制**：
   - 先画到 FBO（离屏）
   - 把 FBO 纹理画到屏幕（预览）
   - 把同一张纹理画到编码器输入 surface（录制）
4) **停止录制**：让编码器 drain 完并 mux 完成

### 为什么要用 FBO（而不是 draw twice）

- **draw twice**：同一套 draw 调用跑两遍（屏幕一次 + 编码器一次）→ 性能浪费、发热更高
- **FBO**：场景只画一次 → 纹理复用两次 → **更省**，也更接近真实工程写法

### 新手最该看的 3 个点

- **FBO 初始化**：理解“颜色附件纹理”+“framebuffer 容器”
- **viewport / letterbox**：为什么录制尺寸和屏幕比例不一致时需要加黑边
- **时间戳**：`setPresentationTime()` 为什么重要（音视频同步/帧顺序）

**下一步**：你已经掌握 Grafika 的“王牌套路”。接下来是“扩展关卡”：摄像头、解码、多 surface、性能调优。

---

## 6. 扩展关卡（按兴趣选学）

### A. 摄像头预览 + 录制

- **推荐入口**：`app/src/main/java/com/android/grafika/CameraCaptureActivity.java`
- **为什么学它**：这是“真实应用”最常见的组合：摄像头 → GL 处理 → 编码
- **下一步**：对照 `CameraPreviewFilter` + `TextureMovieEncoder#setCameraFilterMode`，理解「预览线程改 UI / GL，编码器线程如何收到同一套参数」这条消息链

### B. 解码播放（MediaCodec decoder）

- **推荐入口**：`PlayMovieActivity.java`、`MoviePlayer.java`
- **为什么学它**：编码学会后，再学解码就能拼出“端到端播放器/剪辑器”的基础能力

### C. 多 Surface / 系统合成理解

- **推荐入口**：`MultiSurfaceActivity.java`
- **为什么学它**：理解 SurfaceFlinger/HWC 之后，你会更明白“为什么某些渲染会掉帧/合成开销很大”

### D. 性能与坑点（越早知道越好）

- **线程**：GL 调用必须在 `makeCurrent` 的线程上
- **GC**：稳定态尽量不分配对象（否则容易 jank）
- **分辨率/码率/帧率**：参数不当会导致编码器压力大、掉帧、文件巨大

---

## 7. 给小白的“最小可落地”学习计划（建议 3~7 天）

你可以按下面节奏推进，每天都能“有成果”：

- **Day 1**：跑通 App + 看 `GlesInfoActivity` + 改出一个可见效果（背景色/简单动画）
- **Day 2**：把 `EglCore/WindowSurface` 的“makeCurrent/swapBuffers”搞懂
- **Day 3**：搞懂 `FullFrameRect + Texture2dProgram`（纹理贴屏）
- **Day 4**：搞懂 `VideoEncoderCore` 的输入 Surface + mux 输出
- **Day 5**：精读 `RecordFBOActivity`，把“FBO 一次绘制两处输出”画出来
- **Day 6~7（选做）**：本仓库已把预览滤镜同步进录制；可在此基础上改卷积核、加新滤镜枚举并同时改两处调用

---

## 8. 你卡住时怎么排查（最常见的 5 个问题）

1) **黑屏**：是否 `makeCurrent()`？是否 `glViewport()` 正确？是否 `swapBuffers()`？
2) **录制出来是黑的**：是否把画面画到了编码器 input surface？是否 `setPresentationTime()`？
3) **崩溃/GL 错误**：看 `GlUtil.checkGlError()` 的日志（Grafika 的注释里很强调）
4) **掉帧严重**：检查是否在循环里创建对象（GC），或渲染太重，或编码参数太高
5) **画面拉伸**：屏幕比例 ≠ 视频比例时要做 letterbox/pillarbox

---

## 9. 下一步我可以继续帮你做什么（你选一个目标）

如果你愿意，我可以基于你当前代码直接帮你做一个“小白进阶练习”，比如：

- **练习 1（已实现）**：`CameraPreviewFilter` 统一配置 + `TextureMovieEncoder` 在编码线程应用；你可尝试加「水印层」并同样只在编码路径叠加
- **练习 2**：在 `RecordFBOActivity` 里加一个“叠加水印/文字”的 GL layer，然后一起录制
- **练习 3**：把 `EglCore + WindowSurface` 抽成你自己项目可复用的最小工具库

