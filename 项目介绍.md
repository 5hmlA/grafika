**Grafika 项目完整分析（2026 年最新版，基于 GitHub 官方仓库）**

Grafika 是 Google 官方推出的 **Android 图形与媒体测试应用**，被业界誉为“Android 音视频 + OpenGL ES 进阶圣经”。它不是一个“漂亮的 Demo”，而是一堆“脏活累活”的真实实现，专门用来验证平台 bug、性能问题和最佳实践。

**项目核心信息**（截至 2026 年）：
- GitHub：https://github.com/google/grafika
- Stars：6200+，Forks：1500+
- License：Apache 2.0
- 最低 API：18（Android 4.3）
- 最后提交：2020 年 1 月 18 日
- 2025 年 4 月 15 日正式 **Archived（只读）**，不再接收 PR

它被 Android 官方文档《Android System-Level Graphics Architecture》多次引用，是学习 **TextureView vs SurfaceView**、**MediaCodec Surface 输入**、**EGL 共享上下文**、**FBO 离屏录制** 的最佳教材。

### 项目目录结构（完整）

```
app/src/main/java/com/android/grafika/
├── gles/                     ← 所有 OpenGL 工具类都在这里（最重要！）
│   ├── EglCore.java
│   ├── EglSurfaceBase.java
│   ├── FullFrameRect.java
│   ├── GeneratedMovie.java（部分）
│   ├── Texture2dProgram.java
│   ├── WindowSurface.java
│   └── ...
├── MainActivity.java         ← 入口，列出所有 Demo
├── RecordFBOActivity.java    ← 你最关心的 FBO 录制
├── TextureMovieEncoder.java / TextureMovieEncoder2.java
├── CircularEncoder.java
├── ContinuousCaptureActivity.java
├── CameraCaptureActivity.java
├── PlayMovieActivity.java
├── ...（共 40+ 个 Java 文件）
```

**两大类文件**：
- **Activity 类**：每个都是一个独立 Demo
- **Utility 类**（gles 包 + 顶层）：EglCore、WindowSurface、FullFrameRect、TextureMovieEncoder 等，是整个项目的“心脏”

### 设计理念（Google 官方反复强调）

1. **极致线程安全**  
   MediaCodec + EGL 都是 thread-local 的，稍有不慎就 crash。所有注释里都在提醒“不要跨线程”。

2. **零 GC 抖动**  
   稳态渲染时几乎不 new 对象，所有缓冲区预分配好。这是 Grafika 能做到 60fps 稳定录制的原因。

3. **全 Java，无 NDK**  
   方便小白直接看懂。

### 全部 20+ 个 Activity 完整功能解析（按重要程度排序）

**1. RecordFBOActivity（FBO 离屏录制 —— 重中之重）**  
你上次问的 FBO 就是这里！  
支持**三种录制方式**：
- Method 1：Draw Twice（屏幕 + 编码器各画一次，最简单但性能差）
- Method 2：FBO + Blit（画一次到 FBO，再用 glBlitFramebuffer 或 FullFrameRect 复制到两个 Surface，最推荐）
- Method 3：FBO + on-screen Blit（Grafika 里没完全实现）

核心流程完全匹配我上次给你讲的：  
`prepareFramebuffer()` → `drawScene()` 到 FBO → `FullFrameRect.drawFrame(offscreenTexture)` 到屏幕和编码器 Surface → `swapBuffers()`。

**2. TextureMovieEncoder + TextureMovieEncoder2**  
整个项目最核心的编码器类。  
它把 EGL Context 共享给 MediaCodec 的 input Surface，然后接收 `frameAvailable(SurfaceTexture)` 回调，把 GL 纹理直接喂给编码器。CameraCaptureActivity 和 RecordFBOActivity 都用它。

**3. ContinuousCaptureActivity（环形缓冲录制）**  
最实用的“抖音式录制”：一直录，点按钮才保存最近 7 秒。用了 CircularEncoder + CircularEncoderBuffer。

**4. CameraCaptureActivity**  
前置摄像头预览 + 实时滤镜 + 录制。滤镜只在预览生效，录制是原始画面（经典需求）。

**5. PlayMovieActivity / PlayMovieSurfaceActivity**  
对比 TextureView 和 SurfaceView 播放视频的差异。官方文档里拿来举例。

**6. DoubleDecodeActivity**  
同时解码两个视频到两个 TextureView，演示 SurfaceTexture 复用，避免 decoder 重启。

**7. TextureFromCameraActivity**  
把摄像头作为 OES 外部纹理，用 GLSL 做各种变换（旋转、缩放、滤镜）。

**8. TextureViewGLActivity / TextureViewCanvasActivity**  
教你不用 GLSurfaceView，直接在 TextureView 上做 GL 渲染和 Canvas 渲染。

**9. HardwareScalerActivity**  
演示动态改变 Surface 大小，利用硬件 scaler 提升性能（老项目但技术至今有效）。

**10. ScreenRecordActivity（API 23+）**  
用 MediaProjectionManager 录屏。

其他还有：ColorBar 测试、GLES 信息查询、glTexImage2D/glReadPixels 性能测试、ScheduledSwap（精确帧率控制）等。

### 核心工具类深度解析

- **EglCore**（gles 包）：创建 EGLContext、EGLDisplay、EGLConfig，支持 FLAG_RECORDABLE（MediaCodec 专用）
- **WindowSurface**：对 EGLSurface 的封装，支持 `makeCurrent()`、`swapBuffers()`、`setPresentationTime()`
- **FullFrameRect + Texture2dProgram**：画全屏四边形，把 FBO 纹理 blit 到任意 Surface
- **TextureMovieEncoder**：把 GL 渲染直接编码成 MP4 的“黑盒”，内部包含 VideoEncoderCore

### 如何本地运行（小白专属）

1. `git clone https://github.com/google/grafika.git`
2. 用 Android Studio 直接打开（Gradle 项目）
3. 最低设备：Android 4.3+（推荐 8.0+）
4. 第一次启动会自动生成两个测试视频（gen-eight-rects.mp4 和 gen-slides.mp4）
5. 所有输出文件在 `/data/data/com.android.grafika/files/`

### 学习路线推荐（从小白到高手）

**第 1 周**：先跑 RecordFBOActivity，把三种方式都切一遍，理解 FBO + Blit  
**第 2 周**：看 TextureMovieEncoder → 改成自己的渲染内容  
**第 3 周**：看 ContinuousCaptureActivity → 实现“抖音录制”  
**第 4 周**：TextureFromCameraActivity + 自定义 GLSL 滤镜

**一句话总结**：  
Grafika 就是 Android 图形媒体的“源代码教科书”。它把所有复杂的 EGL + MediaCodec + OpenGL 组合都拆成了最干净的 Demo。只要把 Grafika 吃透，你写任何游戏录制、直播推流、AR 录制、滤镜相机，都能手到擒来。

需要我接下来：
- 单独把 **RecordFBOActivity 完整代码 + 逐行注释** 发给你？
- 把 **EglCore + WindowSurface + FullFrameRect** 三个核心类完整解析 + 简化版单文件示例？
- 还是帮你把 Grafika 改造成“自己的 FBO 录制模板工程”？

随时说，我继续手把手带你！🚀