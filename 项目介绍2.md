**Grafika 项目完整分析（2026 年最新状态）**

Grafika 是 Google 官方推出的 **Android 图形与媒体“实验集合”**（test app / hacks collection），专门用于演示和调试 **OpenGL ES + EGL + MediaCodec** 的各种高级用法。它被广泛视为 Android 音视频/图形开发的“圣经级参考代码”，尤其是你之前问的 **FBO 视频录制**，Grafika 中的 `RecordFBOActivity` 就是最经典、最标准的实现。

### 1. 项目概述
- **官方定位**：一个“垃圾场”（dumping ground），专门扔各种图形媒体 hacks。
- **开发目标 API**：Android 4.3（API 18），支持部分老版本，但部分功能需要 API 19+ / 23+。
- **授权**：Apache 2.0，完全开源，可直接商用。
- **特点**：
    - 强调**线程安全**和**GC 友好**（稳态下几乎零分配）。
    - 所有代码纯 Java（无 NDK）。
    - 配合 Android 系统级图形架构文档（System-Level Graphics Architecture）一起阅读最佳。
- **不是**：稳定产品、官方教程、最佳实践示范（UI 丑、日志啰嗦、边缘 case 处理可能缺失）。

**重要更新**：2025 年 4 月 15 日已被 Google 官方 **归档（read-only）**，不再接受 PR，不再维护。但代码依然经典且可用，6.2k stars，1.5k forks。

### 2. 构建与运行
1. 下载仓库：`https://github.com/google/grafika`（归档前代码）
2. 用 **Android Studio** 直接打开（自带 Gradle wrapper）。
3. 首次启动会自动生成两个测试视频（`/data/data/com.android.grafika/files/` 下）。
4. 主界面 `MainActivity` 列出所有 demo，可通过菜单“Regenerate content”重新生成测试素材。
5. 权限：相机、存储、屏幕录制等（新版已加 `PermissionHelper`）。

### 3. 功能列表（当前所有 Activity）
以下是官方 README 中完整的 20+ 个演示（按重要性排序）：

| Activity | 核心演示内容 | 推荐指数 |
|----------|-------------|----------|
| **Record GL app** (`RecordFBOActivity`) | OpenGL 离屏 FBO 录制（三种方式：draw twice / FBO / GLES3 blit） | ★★★★★（你最需要） |
| Play video (TextureView) | TextureView 播放 MP4（支持 60fps、loop） | ★★★★ |
| Continuous capture | 相机环形缓冲录制（7秒回放） | ★★★★ |
| Show + capture camera | 相机预览 + 实时滤镜 + 录制 | ★★★★ |
| Double decode | 两个视频同时解码到两个 TextureView | ★★★ |
| Live camera (TextureView) | TextureView 直连相机 | ★★★ |
| Screen Record (MediaProjection) | API 23+ 系统屏幕录制 | ★★★ |
| Texture from Camera | 相机 OES 纹理 + GLSL 变换 | ★★★ |
| Hardware scaler exerciser | 动态 Surface 大小缩放 | ★★ |
| Simple GL / Canvas in TextureView | TextureView 基础 GL/Canvas | ★★ |
| ...（其余测试：Color bars、glReadPixels 速度测试、Multi-surface、Scheduled swap 等） | 各种底层测试 | - |

### 4. 目录结构（关键部分）
```
app/src/main/java/com/android/grafika/
├── MainActivity.java                  // 入口列表
├── RecordFBOActivity.java             // FBO 录制核心
├── TextureMovieEncoder.java / TextureMovieEncoder2.java
├── VideoEncoderCore.java
├── CircularEncoder.java
├── gles/                              // EGL + OpenGL 工具包（最重要！）
│   ├── EglCore.java
│   ├── WindowSurface.java
│   ├── OffscreenSurface.java
│   ├── FullFrameRect.java
│   ├── Texture2dProgram.java
│   ├── GlUtil.java
│   ├── Sprite2d.java / Drawable2d.java
│   └── ...
├── CameraUtils.java / MiscUtils.java
└── 各种 Activity...
```

### 5. 核心架构与技术栈
Grafika 的灵魂是 **单 EGLContext + 多 EGLSurface** 设计：
- **EGL 层**：`EglCore` + `WindowSurface`/`OffscreenSurface` 封装所有 EGL 操作。
- **渲染层**：`FullFrameRect` + GLSL Program（`Texture2dProgram` / `FlatShadedProgram`）实现全屏贴图/绘制。
- **编码层**：`TextureMovieEncoder` + `MediaCodec`（Surface 输入）+ `MediaMuxer`。
- **线程模型**：渲染线程（Choreographer/Vsync） + 编码独立线程（Handler + Looper）。
- **关键技巧**：
    - `FLAG_RECORDABLE` 创建可录制的 EGLConfig。
    - 共享 EGLContext（避免上下文切换开销）。
    - FBO / glBlitFramebuffer 避免重复绘制。

### 6. 关键类深度详解

#### **EglCore.java**（EGL 管理核心）
- 作用：创建、管理 EGLDisplay、EGLContext、EGLConfig。
- 关键参数：`sharedContext` + `flags`（`FLAG_RECORDABLE` + `FLAG_TRY_GLES3`）。
- 重要方法：
    - `makeCurrent(EGLSurface)` / `swapBuffers()`
    - `createWindowSurface(Object surface)`（支持 Surface / SurfaceTexture）
    - `setPresentationTime()`（设置帧时间戳给编码器）
- 特点：**非线程安全**，必须在同一个线程操作；自动 fallback GLES3→GLES2。

#### **WindowSurface.java / OffscreenSurface.java**
- 封装 EGLSurface，分别对应显示窗口和离屏 pbuffer。

#### **TextureMovieEncoder.java**（录制编码器）
- 独立线程运行。
- 输入：MediaCodec 的 `createInputSurface()`。
- 输出：MP4 文件（H.264）。
- 支持 `frameAvailableSoon()` + `updateSharedContext()`（适应 SurfaceView 重建）。
- 内部使用 `VideoEncoderCore` 处理 dequeueOutputBuffer + MediaMuxer。

#### **FullFrameRect + Texture2dProgram**
- 一键把任意纹理（包括 FBO 纹理）全屏绘制到任意 EGLSurface。

### 7. 重点案例：RecordFBOActivity（FBO 视频录制）完整拆解
这就是你之前问的“FBO 录制”最标准实现！

**三种录制方式**（Activity 里可切换）：
1. **RECMETHOD_DRAW_TWICE**：画两次（最简单，最耗 GPU）。
2. **RECMETHOD_FBO**（推荐）：先画到离屏 FBO → 用 `FullFrameRect` 分别 blit 到屏幕和编码器 Surface。
3. **RECMETHOD_BLIT_FRAMEBUFFER**（GLES3 最优）：直接画到屏幕，再用 `glBlitFramebuffer` 拷贝到编码器区域（单次渲染 + 单次 blit）。

**核心流程**（跟你上次的代码几乎一致）：
- `prepareGl()` → 创建 EglCore + WindowSurface。
- `prepareFramebuffer()` → 创建 FBO + 颜色纹理 + 深度缓冲。
- `doFrame()`（Choreographer 驱动）：
    - 更新动画（三角形 + 弹跳矩形）。
    - 若录制：`glBindFramebuffer(FBO)` → `drawScene()` → 切回默认帧缓冲 → `FullFrameRect.drawFrame(texture)` 到屏幕和编码器。
    - `setPresentationTime + swapBuffers()` 通知 MediaCodec。
- 使用 `TextureMovieEncoder2`（新版）进行异步编码。

**性能对比**（官方注释）：
- Draw Twice：最费 GPU。
- FBO：平衡。
- BlitFramebuffer：最高效（推荐生产使用）。

### 8. 优缺点与注意事项
**优点**：
- 代码极度“干净”、注释详尽。
- 真实演示了所有坑（线程、GC、EGL 共享、MediaCodec Surface）。
- 直接可运行，适合学习。

**缺点**：
- 已归档，无更新（Android 14/15 新特性不支持）。
- UI 丑陋，部分老设备有兼容性问题（如 Nexus 4 滤镜崩溃）。
- 无音频录制、无 ImageReader 示例。

**常见坑**：
- EGLContext 必须共享，否则报错。
- FBO 必须检查 `GL_FRAMEBUFFER_COMPLETE`。
- 录制分辨率建议 720p/1080p，别用屏幕分辨率（避免浪费）。

### 9. 学习建议
1. **先跑**：直接打开 `RecordFBOActivity`，切换三种模式，观察 logcat 帧率。
2. **再读**：按顺序读 `EglCore` → `WindowSurface` → `RecordFBOActivity` → `TextureMovieEncoder`。
3. **实践**：把 Grafika 的 FBO 部分抽出来做成你的录制库（我上次给你的代码就是基于此简化版）。
4. **进阶**：结合 Camera2 + GLES3 + MediaCodec 做实时美颜录制。

Grafika 虽然归档了，但它的设计思想至今仍是 Android 图形录制的**行业标杆**。你现在已经掌握了 FBO 录制最核心的部分，接下来想加摄像头实时滤镜、加音频、换成 GLES3.2，还是抽成库直接用？告诉我，我继续给你拆代码或写封装版！🚀

需要我把某个具体类的**完整代码注释版**发给你吗？（比如 EglCore 或 RecordFBOActivity 精简可运行单文件版）[项目介绍.md](%E9%A1%B9%E7%9B%AE%E4%BB%8B%E7%BB%8D.md)